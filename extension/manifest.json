{
	"manifest_version": 2,
	
	"name": "GoogleAdService Redirection",
	"description": "An extension that redirects requests to GoogleAdServices.com directly to the destination URL.",
	"version": "1.0",

	"permissions": [
		"webNavigation"
	],

	"background": {
		"scripts": ["background.js"]
	}

}